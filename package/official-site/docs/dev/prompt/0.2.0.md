优化docker构建流程，AI模块新增gpu支持：
1. 现在的Dockerfile是RUN uv sync --extra cpu，固定了只安装cpu版本的依赖，不支持gpu。
2. 新增Dockerfile.gpu，使用RUN uv sync --extra gpu安装gpu版本的依赖。
3. .github\workflows\docker-build-push-ai.yml新增gpu构建流程，用于构建包含gpu支持的docker镜像，镜像名后面添加-gpu。

---

搜索功能优化：
1. 现在搜索功能仅支持以文搜图，需要支持按照OCR文字、地理位置、人物名称、相册名、文件夹名、文件名等进行搜索。
2. 输入文字之后，显示候选项，用户可以选择其中一个进行搜索。
   1. 候选项包括OCR文字、地理位置、人物名称、相册名、文件夹名、文件名等，只有对应项有搜索结果才会显示。
   2. 用户可以根据自己的需求选择其中一个进行搜索。
   3. 以文搜图功能（画面识别）放在第一个候选项必须有，用户可以直接输入文字进行搜索。

搜索功能优化：
1. 按图片中文字搜索，应该只有一个候选项：“图片中包含文字：xxx”，点击之后，会跳转到搜索结果页面，显示所有包含xxx文字的图片（现在是返回很多个候选项，用户需要手动筛选选择某一个文字，不方便）。
2. 移动端优化：
   1. 点击搜索按钮之后，用一个新页面，显示独立的搜索框和候选项列表，而不是在当前页面显示（输入框太小）。

---

bug修复：
1. 搜索文件夹失效
2. 按地点搜索不只有city，还可以搜索country、province、district、address等。
3. 支持按标签、景区等进行搜索

写一个Python脚本，读取package\server\reverse_geocoder\rg_cities1000.csv文件，

```csv
lat,lon,name,admin1,admin2,cc
42.53176,1.56654,Vila,Encamp,,AD
42.57952,1.65362,El Tarter,Canillo,,AD
```

---

首先过滤掉cc为CN的行，然后把结果写入到写一个Python脚本，读取package\server\reverse_geocoder\rg_cities1000_1.csv文件中
然后修改表头为longitude,latitude,country,admin_1,admin_2,admin_3,admin_4
其中：
- longitude对应原lon列
- latitude对应原lat列
- country对应原cc列
- admin_1对应原admin1列
- admin_2对应原admin2列
- admin_3对应原name列
- admin_4为空

---

修改下载与加载功能：
1. 指定文件夹，从文件夹下加载所有可用的csv文件，每个文件对应一个国家的城市数据，表头必要包含需要的数据，加载失败给出日志提示。
2. 新增下载某个国家的数据功能，可以根据国家代码下载对应的城市数据，下载到指定文件夹。
3. 把功能提取到单独的Python文件里，init.py只保留必要的导入功能

---

实现某个国家的数据下载与加载功能：
package\server\reverse_geocoder\__init__.py里实现了download和load函数，分别用于下载和加载城市数据。
package\server\resources\rg_data文件夹用于存储下载的城市数据文件。
1. 设置-》基础设置（package\website\src\views\settings\BasicSettings.vue）的地图配置里新增一个国家选择框，用户可以选择要下载的国家。
2. 显示已经下载的国家列表，用户可以在选择框中选择已下载的国家，下载对应的城市数据。
3. 用户可以自己上传自己的城市数据文件，文件格式与rg_cities1000_1.csv相同，上传后需要显示在已下载国家列表中（必须校验文件格式是否正确，如果失败给出对应提示）。

前端设置页面中可以通过点击已下载的国家数据通过浏览器下载到本地查看

在package\official-site里新增关于设置离线地图数据的文档说明（中英文都有）

package\website\src\views\settings\BasicSettings.vue 优化下载按钮的显示与交互，点击“下载到本地”按钮后，有些文件比较大，下载完成之后才会有反应，用户会以为卡了，需要优化交互过程

package\website\src\views\settings\BasicSettings.vue 新增删除已下载的国家数据功能，删除的时候需要确认，删除成功后需要从已下载国家列表中移除。

---

自定义相册新增添加图片图片功能：
现在用户只能在其他地方选择图片添加到自定义相册中，而不能直接在自定义相册中添加图片。
1. 新增一个添加按钮，点击后可以打开图片选择器，用户可以选择要添加的图片，添加到自定义相册中。
2. 仅自定义的普通相册可以添加图片，智能相册、条件相册都不能添加
3. 图片选择器可以直接使用package\website\src\views\PhotosPage.vue

bug修复：
1. package\website\src\views\PhotosPage.vue是用来显示全部照片的，其原有功能不要改变
2. 点击添加照片之后显示暂无照片，且不能点击关闭按钮

bug修复：
1. package\website\src\components\PhotoGallery.vue原有的选择功能失效了
2. package\website\src\components\UnifiedPhotoPage.vue里面 UnifiedPhotoPage.vue 206-208 albums未定义
3. 点击添加照片还是不显示全部图片

---

位置相册景区展示优化：
1. 网格视图和地图视图新增“已打卡、未打卡、全部”三个筛选选项，用户可以根据自己的需求选择显示已打卡、未打卡或全部景区。
2. 新增景区按钮位置调整到左侧
3. 新增删除景区功能

网格视图获取的景区接口是有照片的景区，没有照片的景区不在返回的数据中，所以未打卡选项不显示任何内容
1. 网格视图和地图视图使用同一个接口，返回所有景区的信息，包括有照片的和没有照片的，有照片的景区返回一张照片作为该景区的封面。
2. 网格视图下，有封面的景区显示封面，没有封面的景区显示一个默认的图标。
3. 地图视图下，有照片的景区鼠标悬浮时显示封面，点击会显示该景区的所有照片

---

优化车票管理Ui：
1. package\website\src\views\TicketPage.vue 中顶部导航栏太占空间，需要优化布局，去掉多余的按钮，例如右侧头像
2. package\website\src\views\TicketPage.vue 新增一个“统计按钮”，点击后跳转到package\website\src\views\StatisticsPage.vue，去掉顶部导航栏的统计选项
3. 新增导入导出功能，用户可以导入自己的车票数据，也可以导出自己的车票数据，导入导出的文件格式为csv或者JSON（server的api已经实现）
4. 把车票相关的页面放到一个单独的文件夹里，方便管理
