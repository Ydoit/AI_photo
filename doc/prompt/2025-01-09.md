官网页面修改

全面更新package/official-site目录下的项目内容，包括但不限于以下具体任务：
1. 详细梳理并掌握整个项目的架构和工作流程，包括：
       - 项目目录结构
       - 构建部署流程
       - 前后端交互机制
       - 现有功能模块
2. 将当前开发的车票识别演示功能完整集成到项目中，具体要求：
   - 设计合理的UI界面，确保与现有网站风格一致
   - 实现图片上传、识别结果显示等完整交互流程
   - 添加必要的错误处理和用户引导
3. 图片资源处理：
   - 评估并确定所需演示图片的类型和数量
   - 如需额外图片素材，将向您提出具体需求清单
   - 优化图片加载和显示性能
4. 前端集成要求：
   - 确保功能完美融入现有网页布局
   - 保持响应式设计，适配不同设备
   - 优化用户体验和交互流畅度
5. 质量保证：
   - 进行全面的功能测试
   - 检查与其他模块的兼容性
   - 确保部署后运行稳定

将当前演示中的图片上传功能替换为直接使用车票原票和检测效果两张图片进行展示。具体要求如下：
1. 移除原有的图片上传界面和功能组件
2. 在展示区域预置两张固定图片：
   - 左侧显示车票原票图片
   - 右侧显示当前页面的检测效果图片
3. 确保图片展示满足以下要求：
   - 保持原始图片比例不失真
   - 图片分辨率清晰可见
   - 两图并排显示，间距适中
   - 添加适当的标题说明（"车票原票"和"检测效果"）
4. 实现响应式布局，在不同屏幕尺寸下都能正常显示
5. 添加图片放大查看功能，点击图片可查看大图


修改当前项目中的ai/ticket_service.py文件，实现以下功能改进：

1. 在处理YOLO检测结果时，添加置信度过滤逻辑，只保留confidence值大于0.6的检测结果
2. 修改数据传递流程，确保只有符合置信度要求的检测结果会被传递给后续OCR处理模块
3. 在过滤逻辑中需要包含以下具体实现：
- 解析YOLO检测结果中的confidence字段
- 实现阈值比较判断（confidence > 0.6）
- 过滤掉不符合条件的检测结果
4. 保持原有文件的其他功能不变，仅针对YOLO到OCR的数据传递部分进行修改
5. 添加必要的日志记录，记录被过滤掉的低置信度检测结果数量